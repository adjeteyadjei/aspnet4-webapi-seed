using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace WebApiSeed.Services
{
    public class ServicesScheduler
    {
        public static void Start()
        {
            //IScheduler scheduler = StdSchedulerFactory.GetDefaultScheduler();
            //scheduler.Start();

            //var birthdayService = JobBuilder.Create<BirthdayWishService>().Build();

            //ITrigger trigger = TriggerBuilder.Create()
            //    .WithDailyTimeIntervalSchedule
            //      (s =>
            //         s.WithIntervalInHours(24)
            //        .OnEveryDay()
            //        .StartingDailyAt(TimeOfDay.HourAndMinuteOfDay(6, 0))
            //      )
            //    .Build();

            //scheduler.ScheduleJob(birthdayService, trigger);
        }
    }
}